# Day10 Linux パッケージ管理

## 環境

OS : Ubuntu  
環境 : VirtualBox  

## 実行コマンド

```bash
sudo apt update
sudo apt upgrade
sudo apt install tree
tree
sudo apt remove tree
```

## 学び

apt updateでパッケージ一覧を更新できる。  
apt upgradeでインストール済みソフトを更新できる。  
apt installでソフトをインストールできる。  
apt removeでソフトを削除できる。
